# Provider Audit and Approval

Every provider operation records an audit event. Blocked operations are audited too.

Apply requires an approval id beginning with `approved_`.

```bash
make provider-audit
```
