# v0.1.4 Release Candidate

## Version Bump
The `pyproject.toml` and `zai_coder/__init__.py` versions have been officially bumped to `0.1.4`.

## Scope
This Release Candidate introduces the Automated Agent Runners phase, bringing robust self-queue execution, reliable safe runner policies, subagent delegation, local context references, toolset mapping, and MCP bridging capabilities without violating the core ZAI safety ethos.

## Local Package Validation
All package checks are successfully completed. Artifacts are safely stored in local-only directories without pushing binaries to Git.

## Pending Final Steps (Deferred)
- Create signed Git tag `v0.1.4`.
- Generate the verified GitHub Release.
- Upload the `dist/` tar/zip artifacts directly to GitHub assets.
