# v0.1.3 Patch Cycle Planning

## Current Baseline
- **main HEAD**: `ea5748c`
- **Current project version**: `0.1.2`
- **Previous release**: `v0.1.2`

## Release Type
- **Patch Release** (incremental bugfixes and diagnostics hardening)

## Non-Goals
- No version bump to `0.1.3` during the planning phase.
- No GPG `v0.1.3` release tag creation.
- No public API stabilization claims or guarantees.
- No breaking architectural changes.
- No external network requirements.

## Proposed Scope
1. **CLI UX Polish & Subcommands Consistency**:
   - Align help screens and parameter validations across subcommands (`doctor`, `ask`, `serve`).
2. **Safe Runner & Policy Registry Regression Expansion**:
   - Build extensive testing around `SafeRunner` and `SafetyPolicy` matching engines (including nested exclusions and path validations).
3. **Install/Uninstall Diagnostics Hardening**:
   - Expand unit tests mapping edge cases for dry-run vs applied installations.
4. **Release Automation Dry-Run Hardening**:
   - Strengthen the release scripts dry-run path configurations (`package.sh` and `package-check.sh`).
5. **Generated-File Exclusion Harnessing**:
   - Write automated unit tests to verify that temporary caches, databases, and build files are correctly ignored.
6. **Local-First Docs Cleanup**:
   - Perform routine documentation cleanup to enforce local-first/stdlib-first design rules.

## Candidate PR Breakdown
- **PR A**: CLI UX polish and help diagnostics tests.
- **PR B**: Safe-runner and policy-registry regression tests.
- **PR C**: Install/uninstall diagnostics hardening.
- **PR D**: Release dry-run automation hardening.
- **PR E**: v0.1.3 release bump and tag preparation.

## Acceptance Gates
- Pytest passes with `0 warnings`.
- Safety check, repo check, and secret scans pass.
- Package check verifies `0.1.2` build correctly (and subsequent version during release PR).
- Exclusion audit finds zero untracked `.db` or packaging assets.

## Risk Register & Mitigations
- **Unintended API Exposure**: Keep all server and update framework modules private/internal.
- **Accidental Artifact Commits**: Automate checks in pre-commit/pre-push hooks to flag any `.db` or `.zip`/`.tgz` files.
- **Release Automation Mutations**: Ensure dry-run actions do not trigger remote pushes or tags.
