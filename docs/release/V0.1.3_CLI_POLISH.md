# v0.1.3 CLI UX Polish and Help Consistency

## Scope
Improve top-level and subcommand help menus in a patch-safe way, reporting `0.1.2` as the current version with a clean `--version` argument option. Ensure all invalid choices or arguments raise proper exits with clean diagnostics.

## Commands Inventoried
All subparsers listed in the primary parser configuration:
* `doctor`
* `agents`
* `skills`
* `ask`
* `chat`
* `plan`
* `run`
* `scan`
* `audit`
* `metrics`
* `eval`
* `bench`
* `deploy`
* `serve`
* `memory`
* `patch`
* `self`
* `repair`
* `media`
* `tui`
* `index`
* `rag`
* `task`
* `policy`
* `migrate`
* `update`

## Help/UX Changes
* Added `--version` yielding current static version `0.1.2` (no bump).
* Normalised all command subparser descriptions by supplying explicit `help="..."` parameters.
* Ensured top-level layout exhibits clear and tidy alignment of options and subcommands.

## Tests Added
* `tests/test_cli_help_v013.py`

## Compatibility Notes
* No existing command strings renamed.
* No option parameters removed.
* No external libraries introduced.

## Validation Results
* Target test suite passed successfully.
* All baseline test suites passed.
* Generated exclusions verify no untracked artifact databases.
