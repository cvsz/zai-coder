# v0.1.6 Roadmap Planning

## Focus Areas
1. **Production-Ready Enterprise Upgrades (Deep Scan)**
   - Scan for mocked implementations, `TODO`s, and missing concrete logic.
   - Upgrade `audio_capture.py` from mocked wave headers to a true plugin interface for `sounddevice` or `pyaudio`.
   - Upgrade `media_viewer.py` to support real terminal image rendering using standard protocols (e.g., kitty image protocol or sixel) if the environment supports it, or robust fallback text.
2. **Advanced MCP Integrations**
   - Expand the SQLite inspector with robust validation.
   - Create generic HTTP and REST API MCP abstractions.
3. **Resiliency and Metrics**
   - Solidify the scheduler to operate via an active daemon or reliable background process if permitted by the user policy.
   - Introduce detailed telemetry exporters (Prometheus format) from the local SQLite tracker.

## Next Steps
- Create `V0.1.6_TASK_CHECKLIST.md` to track these efforts.
- Begin the "Enterprise Upgrade Deep Scan" phase.
