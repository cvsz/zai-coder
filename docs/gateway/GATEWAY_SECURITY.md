# Gateway Security

Defaults:

- auth required by default
- tenant context required by default
- security headers enabled
- CORS defaults to localhost
- rate-limit policy attached per route
- audit redacts authorization, cookies, and API keys
