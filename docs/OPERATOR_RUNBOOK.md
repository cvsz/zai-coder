# Operator Runbook
- **Failed Tasks**: Identify failures using `./zai-coder task logs <id>`
- **Database Re-Index**: `./zai-coder index clear --apply` followed by `./zai-coder index build`
- **TUI Mode**: Access the operator gateway via `./run.sh tui`
