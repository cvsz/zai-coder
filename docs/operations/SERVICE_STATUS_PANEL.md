# Service Status Panel

The service status panel summarizes:

- local FastAPI service
- systemd unit
- Docker Compose deployment
- PostgreSQL backend
- Cloudflare Access

Use:

```bash
make ops-status-panel
```
